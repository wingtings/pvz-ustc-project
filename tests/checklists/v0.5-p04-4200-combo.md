# v0.5 P04 4200 组合实机验证

测试日期：2026-08-30

状态：补丁 EXE 与累计 PAK 的组合切片通过；4200 是 v0.3 的临时验证值，不代表最终平衡已经定稿。

## 测试组合

| 对象 | SHA-256 |
| --- | --- |
| 基线 EXE | `6F1729369AC9C5F859E8F3B55FE7D513FBC20B5C54127FD3A1C7E500237FDE6F` |
| v0.3 临时补丁 EXE | `CB0E0FFF4A530164582E3E0A86FA046A5217AC4293F961A75FB3097C853899B8` |
| 基线 PAK | `3B5291C6600076AAF1791AE1FB2DBF247290A23E903D1D376413DA17358E049D` |
| P01+P02+P04 累计 PAK | `ECDBA376D631EA59F738E35AA5F78C36722AA20A5EEC0339900A4433ED0CB12B` |

场景使用迷你游戏“谁笑到最后”。测试时从 `dist/v0.3` 启动补丁副本，并把工作目录指向仓库根目录，因此程序读取的是临时换入的累计 PAK。仓库根目录中的基线 EXE 没有被覆盖。

## 数值观察

测试只读取进程内已提交、可读的私有内存，没有写入游戏进程。扫描限制为四字节对齐的 32 位整数，目的是排除字符串或未对齐字节碰巧组成 4200 的情况。

| 时点 | 当前耐久 | 最大耐久 | 说明 |
| --- | ---: | ---: | --- |
| 放置前 | 未找到 | 未找到 | 进程中没有四字节对齐的 4200 |
| 第一株刚放下 | 4200 | 4200 | 出现两个相邻值 |
| 第一株受击 | 3608 | 4200 | 当前值下降，最大值不变 |
| 第一株重伤 | 940 | 4200 | 重伤贴图已出现 |
| 第一株被吃完 | 0 | 4200 | 沿用原版消失流程 |
| 第二株刚放下 | 4200 | 4200 | 新对象在另一组临时地址生成 |
| 第二株暂停观察 | 1412 | 4200 | 仍在场，最大值没有随受击变化 |

两次放置得到的地址不同，说明这些地址只适合作为本次运行的观察点，不能写进补丁。真正可重复的修改仍是文件偏移 `0x0005E1A7` 处的 `A0 0F 00 00` → `68 10 00 00`，由补丁清单、旧字节检查和唯一 AOB 负责定位。

## 画面观察

- 选卡栏显示灰褐墙体、蓝色“14”编号牌和费用 50。
- 完整主体落地后锚点正常，编号牌没有离开左下角。
- 持续啃食时依次看到了轻伤、重伤和消失；三张累计 PAK 资源能在 4200 最大耐久下正常切换。
- 1412/4200 与 940/4200 两个切片分别落在不同受损外观中。此次没有逐帧搜索精确切换边界，因此不把这两个数写成阈值。

三档外观的清晰对照仍使用 [`p04-damage-stages-v01.png`](../../docs/images/v0.5/p04-damage-stages-v01.png)。那张图来自原版耐久基线；本记录补上的是“4200 初始化补丁与同一套三档资源能够一起工作”的证据。

## 自动核验

| 检查 | 结果 |
| --- | --- |
| 完整 Python 测试集 | 63 项通过 |
| v0.3 二进制补丁预演 | 两处补丁定位与结果哈希通过，未写文件 |
| P01+P02+P04 累计 PAK 预演 | 8 / 2413 项替换通过，结果哈希一致，未写文件 |

## 恢复核验

测试结束后只停止了本次启动的补丁进程，并从测试前备份恢复 PAK、五个存档文件和注册表窗口设置。

| 检查项 | 恢复结果 |
| --- | --- |
| 根目录 EXE | `6F1729369AC9C5F859E8F3B55FE7D513FBC20B5C54127FD3A1C7E500237FDE6F` |
| 根目录 PAK | `3B5291C6600076AAF1791AE1FB2DBF247290A23E903D1D376413DA17358E049D` |
| `game1_0.dat` | 与测试前备份一致，`2AAE128C5A2E588B200BBEDF0EBBA628495B75F9A96EEDD3B9ED2679257A2416` |
| `log.txt` | 与测试前备份一致，`E94BB5CBD29F9E1F358E4569081BF72D638E0E6ADB32CA3E7BA3456ED772F515` |
| `stat1.txt` | 与测试前备份一致，`77B16A31EA014056B47193D4939458BF93120737F26B3098584C9810A2143169` |
| `user1.dat` | 与测试前备份一致，`7D8CA79F0EFA2B168A8DCEEF89E0FB3C61A64C6B062AEB5F43CB2611B55091F6` |
| `users.dat` | 与测试前备份一致，`7812F2BAF43689ED0DC9D077ECEAFA220EF3AF54EF33CE9CAEA1DD3C75BD9651` |
| 窗口设置 | `ScreenMode=0`、`PreferredX=1746`、`PreferredY=451` |
| 进程 | 没有残留 `PlantsVsZombies*` 进程 |

## 仍未覆盖

- 没有把“谁笑到最后”完整打完，也没有做新存档通关。
- 没有在夜晚、普通水池、雾夜和屋顶复测 P04 的对比度与受伤切换。
- 没有逐点测出轻伤、重伤的精确数值边界。
- 4200 只用于证明初始化补丁；正式平衡仍以植物原型和后续试玩为准。

因此，这次结果可以关闭“P04 4200 是否真的进入新对象、是否能与三档贴图共存”两项问题，不能替代跨场景和完整关卡回归。
