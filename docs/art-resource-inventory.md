# v0.2.1 首批美术资源清单

状态：资源清单和首批五槽位的 12 份像素契约已完成；P01、P02、P04 已完成首轮白天观察，Z03 已通过图鉴与泳池四阶段实机切片，Z01 尚未制作候选。

盘点日期：2026-08-30

适用基线：PC 版 `1.0.0.1051`，`main.pak` SHA-256 为 `3B5291C6600076AAF1791AE1FB2DBF247290A23E903D1D376413DA17358E049D`。

## 盘点结论

- 当前 PAK 共 2413 个资源。仓库脚本会先核对哈希，再列出或选择性提取文件。
- P01、P02、P04 分别使用独立的植物动画；Z01 与 Z03 共用 `Zombie.reanim.compiled`，书套头通过其中的 `anim_cone` 轨道挂载。
- 选卡和图鉴没有为每个单位保存一张独立大图。`SeedPacket_Larger.png`、`Almanac_PlantCard.png` 和 `Almanac_ZombieCard.png` 是通用框，角色本体仍由动画部件绘制。因此替换角色部件后，选卡和图鉴也要一起复查。
- 四份主动画都能从 PC 压缩格式读成 Raw XML，帧率均为 12 FPS。第三方转换工具只用于这次结构核对，尚未固定版本，也不进入正式构建依赖。
- 原版 PNG 和解码后的动画只保存在被 Git 忽略的 `.work/pak-reference`。仓库只提交清单、工具、测试记录和原创概念稿。
- 12 份契约统一登记在 [`v0.5-first-slice-contracts.json`](../patches/manifests/v0.5-first-slice-contracts.json)；登记项与磁盘契约、PAK 目标和五个槽位必须完全一致。

## 共用美术规则

1. 保留原版单位的外轮廓、朝向、落脚点和动作节奏，先做同尺寸部件替换。
2. 科大感来自眼镜、蓝白配色、书本、草稿纸、校园卡和实验器材，不直接贴中国科大校徽，也不让角色影射具体师生。
3. 小字号汉字在最终部件中手工排版。生成式概念图只负责造型方向，不作为游戏贴图直接缩放。
4. 第一轮沿用原版粒子与音效。只有识别物稳定后，才单独替换绿圈、专注值和书套碎屑。
5. 每个替换件必须保留透明边界，并记录原尺寸、锚点、前后层关系和受伤阶段。

## 五个首发槽位

### P01 绿圈科豆

| 项目 | 记录 |
| --- | --- |
| 原槽位 | `PEASHOOTER` |
| 动画 | `compiled/reanim/PeaShooter.reanim.compiled`：24 条轨道、19 个实际图像引用；另有 21 张 `PeaShooter_*.png` 和精简动画 `PeaShooterSingle.reanim.compiled` |
| 必换部件 | `PeaShooter_Head.png` 增加小圆框眼镜；`PeaShooter_frontleaf.png` 或独立书本部件负责托住蓝色《电磁学千题解》；后续把 `ProjectilePea.png` 改成绿色圆圈 |
| 可复用 | 茎、后叶、头顶叶、嘴、眨眼、`PeaSplat` 粒子和原发射音效 |
| 轮廓与主色 | 保留原版右向喷口和上下叶片；原版亮绿为主体，深蓝书本、黑色眼镜、少量蓝白书签为识别点 |
| 选卡与图鉴 | 由同一套 reanim 缩放绘制，没有单独角色卡图；书名缩小时不要求逐字可读，但蓝书和眼镜必须可辨 |
| 受伤与死亡 | 原槽位没有分段受伤贴图；被吃掉时沿用普通植物消失流程 |
| 是否改 reanim | 眼镜可直接合入头部，不必改动画。书本若合入前叶且不越界也可不改；若要让书稳定跟随叶片并保持正确遮挡，则新增独立轨道 |

首张方向稿见[绿圈科豆概念稿](../assets-src/concepts/p01-greencircle-pea-concept.png)，生成记录与完整提示见[概念稿说明](../assets-src/concepts/p01-greencircle-pea-concept.md)。它是造型参考，不是能直接覆盖 70×65 头部贴图的成品。

头部原件的尺寸、哈希、Alpha 蒙版、眼镜允许区域和瞳孔保护区已经写入 [`PeaShooter_Head.contract.json`](../assets-src/game/p01/PeaShooter_Head.contract.json)。正式候选图必须通过该契约，不能把放大重绘或不透明背景缩回 70×65 冒充原尺寸修改。

前叶原件为 67×40，中央空间足以放入缩放后仍可辨认的竖向蓝书。首版决定把书本合入前叶，不增加 reanim 轨道；[`PeaShooter_frontleaf.contract.json`](../assets-src/game/p01/PeaShooter_frontleaf.contract.json) 允许在中央新增书本 Alpha，但禁止削掉原叶片，并检查深蓝封面与浅色书页像素。

首轮实机已经确认这两个部件能随原 reanim 正常待机和发射：眼镜没有遮住喷口或改变头部轮廓，蓝书随前叶留在中央，选卡和图鉴的缩放绘制也能看到两个识别点。实机截图与仍待检查的眨眼、跨场景项目见 [`v0.5-p01-ingame.md`](../tests/checklists/v0.5-p01-ingame.md)。

### P02 学习蝌蝻

| 项目 | 记录 |
| --- | --- |
| 原槽位 | `SUNFLOWER` |
| 动画 | `compiled/reanim/SunFlower.reanim.compiled`：29 条轨道、30 个图像引用；PAK 内有 29 张 `SunFlower_*.png`，并复用豌豆射手的茎叶部件 |
| 必换部件 | 头部改为专注学习的表情；花瓣改成蓝白便签或书页意象；在不破坏轮廓的前提下加入校园卡或小水杯 |
| 可复用 | 茎叶、眨眼、摆动节奏、生产动作和原音效；第一轮仍用 `Sun.reanim.compiled` 作为专注值占位 |
| 轮廓与主色 | 保留圆形花盘和放射花瓣；暖黄主体配蓝白纸张边缘，不能全改成冷色而丢掉经济单位识别度 |
| 选卡与图鉴 | 共用角色动画；要分别检查普通待机、生产瞬间和图鉴放大状态 |
| 受伤与死亡 | 无分段受伤图；沿用普通植物消失流程 |
| 是否改 reanim | 同尺寸换头和花瓣不需要。若增加能独立摆动的校园卡、水杯或草稿纸，则需要新增轨道 |

首轮不改全部 29 张资源。当前战斗 reanim 只引用基础头部而不引用 PAK 中的 `head_sing1..5` 和 `head_wink`；因此先用 [`SunFlower_head.contract.json`](../assets-src/game/p02/SunFlower_head.contract.json) 保持眼睛与眨眼对位，在眉形和脸侧加入专注感、科大蓝小标记。顶部与底部成组花瓣分别由 [`SunFlower_toppetals.contract.json`](../assets-src/game/p02/SunFlower_toppetals.contract.json) 和 [`SunFlower_bottompetals.contract.json`](../assets-src/game/p02/SunFlower_bottompetals.contract.json) 改成蓝白便签，其余花瓣保留暖黄。

当前差分稿把科大蓝标记做成窄学习头带，并用两条向内收的眉形表达专注；上下成组花瓣分别采用白纸/蓝纸和蓝纸/白纸分区。白天生存关已经覆盖选卡、落地待机、产出瞬间和图鉴放大，头带与蓝白花瓣能随原动画稳定摆动。单独眨眼帧、完整一波和其余场景仍未观察，因此暂不扩改另外 17 张独立花瓣。

### P04 出恭墙

| 项目 | 记录 |
| --- | --- |
| 原槽位 | `WALL_NUT` |
| 动画 | `compiled/reanim/Wallnut.reanim.compiled`：6 条轨道、4 个动画图像引用；共有 6 张 PNG |
| 必换部件 | `Wallnut_body.png`、`Wallnut_cracked1.png`、`Wallnut_cracked2.png` 三张 100×100 主体图，统一改成一教门口老墙或石墩意象 |
| 可复用 | 两张眨眼、抽动部件、啃食粒子与受击音效；眼睛可以先保留原版表演性 |
| 轮廓与主色 | 保留近似椭圆的高耐久剪影；灰褐墙体、浅色粉刷层和蓝色小编号牌，避免画成普通砖块后认不出槽位 |
| 选卡与图鉴 | 共用角色动画；三档主体必须在卡片缩略图和战场 100% 比例下都能区分 |
| 受伤与死亡 | 耐久阈值由程序切换 `body`、`cracked1`、`cracked2`；裂纹位置必须连续，粒子第一轮复用 |
| 是否改 reanim | 不需要。保持三张主体图均为 100×100，透明外边界和原锚点不变即可 |

三档主体已经分别建立 [`Wallnut_body.contract.json`](../assets-src/game/p04/Wallnut_body.contract.json)、[`Wallnut_cracked1.contract.json`](../assets-src/game/p04/Wallnut_cracked1.contract.json) 和 [`Wallnut_cracked2.contract.json`](../assets-src/game/p04/Wallnut_cracked2.contract.json)。首版保留原 Alpha 缺口与脸部表演，只重绘外壳；契约要求灰褐墙面与蓝色编号牌同时存在，并防止三档资源串装。

首轮差分稿已经完成。外壳按原明暗改成灰褐石墙，砖缝在三档间沿用同一走向，左下角固定一块蓝色“14”编号牌。最初用矩形保护眼睛和嘴时会留下突兀的黄块，现已改成按原色保护深色线稿、裂纹和眼白，因此脸部与损伤细节能自然留在新墙面上。三张候选保持原 Alpha，分别改动 5359、5184 和 4734 个可见像素。白天生存关已确认选卡、图鉴、完整外观和两次受伤切换；裂口逐档扩大时没有跳位。随后又把累计 PAK 与 v0.3 临时 4200 耐久补丁一起运行，当前耐久下降时最大耐久保持 4200，三档仍能正常切换。静态结果见 [`v0.5-p04-static.md`](../tests/checklists/v0.5-p04-static.md)，原版耐久实机记录见 [`v0.5-p02-p04-ingame.md`](../tests/checklists/v0.5-p02-p04-ingame.md)，组合结果见 [`v0.5-p04-4200-combo.md`](../tests/checklists/v0.5-p04-4200-combo.md)。

### Z01 普通微积分

| 项目 | 记录 |
| --- | --- |
| 原槽位 | `ZOMBIE` |
| 动画 | 与多种基础敌人共用 `compiled/reanim/Zombie.reanim.compiled`：42 条轨道、37 个图像引用，包含行走、啃食、死亡、游泳及若干装备轨道 |
| 必换部件 | 优先替换头、下颌、领带和躯干衣服；把破西装改成写满极限与导数符号的旧卷面，四肢先沿用原版 |
| 可复用 | 行走、啃食、掉头、断臂、倒地动画，基础粒子和全部音效 |
| 轮廓与主色 | 保留含胸前倾的基础敌人剪影；旧纸白、铅笔灰和批改红为主，脸和手仍保留原版灰绿，避免变成一张会走的矩形试卷 |
| 选卡与图鉴 | 敌人图鉴同样绘制共享 reanim；必须确认无装备状态不会误显示书套或铁桶轨道 |
| 受伤与死亡 | 头、手臂等脱落继续使用共享部件和原死亡轨道；换头后要同步检查掉头画面 |
| 是否改 reanim | 第一轮不需要。按原尺寸替换共享部件即可，但任何修改都会影响调用同一基础部件的其他敌人，必须做跨槽位回归 |

首轮决定只改 [`Zombie_body.contract.json`](../assets-src/game/z01/Zombie_body.contract.json)：保留 53×63 画布、Alpha 和原深色线稿，把衣服改成旧卷面并加入铅笔灰、批改红。头、下颌与领带先复用原版，避免在没有跨槽位截图的情况下同时改变脸、脱落头和所有装备型敌人。该躯干仍会影响 Z03、Z05 等共享基础骨架的槽位，实机验收必须覆盖装备存在、受损和脱落三种状态。

### Z03 B 系列淑芬

| 项目 | 记录 |
| --- | --- |
| 原槽位 | `CONEHEAD_ZOMBIE` |
| 动画 | 共用 `Zombie.reanim.compiled` 的 `anim_cone` 轨道；书套对应 `Zombie_cone1.png`、`Zombie_cone2.png`、`Zombie_cone3.png`，三张均为 59×57 |
| 必换部件 | 把三个路障受损阶段改成逐步破裂、散页的厚蓝书套；本体沿用 Z01 的普通微积分视觉 |
| 可复用 | 基础敌人全部动作、`ZombieTrafficCone.xml.compiled` 粒子与音效；首轮粒子可暂不改色 |
| 轮廓与主色 | 书套仍占据头顶路障的大体体积，深蓝封面、白色书脊、少量折角和便签；破损越重，露出的卷页越多 |
| 选卡与图鉴 | 图鉴必须显示完整书套；实战检查三档耐久切换和书套脱落后恢复 Z01 本体 |
| 受伤与死亡 | 370 点附加耐久由三张书套图表达，脱落后继续使用 270 点本体和基础死亡动画 |
| 是否改 reanim | 不需要。三张书套严格保留 59×57 画布和原锚点即可；只有书页要独立飘落时才改粒子或动画 |

三档书套已经分别建立 [`Zombie_cone1.contract.json`](../assets-src/game/z03/Zombie_cone1.contract.json)、[`Zombie_cone2.contract.json`](../assets-src/game/z03/Zombie_cone2.contract.json) 和 [`Zombie_cone3.contract.json`](../assets-src/game/z03/Zombie_cone3.contract.json)。它们保留下沿头部接触带，允许上半部从三角路障受控改成倾斜书套，并要求白页暴露量随损伤递增；只把路障涂成蓝色不能通过。

首轮静态稿已经完成。三档都把路障上半部重构为倾斜的厚蓝习题册，完整阶段保留白色“B”题签和一小段批改红，受损后封面逐步撕开、浅色页块不断露出。三张图仍是 59×57，也逐像素保留了 `y=43..56` 的头部接触带；露页像素为 481、652、726，可见轮廓则从 1654 递减到 1504、1334。候选已经通过受控轮廓契约，并进入 11 项替换的累计 PAK。

图鉴放大能看清蓝色封面和白色“B”题签；“谁笑到最后”的泳池战场也依次出现了完整、轻伤、重伤和脱落。行走、啃食和阶段切换没有黑块或锚点跳动。测试包没有修改共享 reanim，也没有装入 Z01 躯干候选，所以书套脱落后显示的仍是原版基础僵尸。这证明装备层能正常清除，不代表 Z01 已经完成。静态数据见 [`v0.5-z03-static.md`](../tests/checklists/v0.5-z03-static.md)，截图与恢复记录见 [`v0.5-z03-ingame.md`](../tests/checklists/v0.5-z03-ingame.md)。

## 首批制作顺序

1. 先把 P01 的眼镜合入头部，原尺寸替换并验证眨眼、发射和图鉴。
2. 再制作 P01 的前叶合成书本；若实机遮挡不成立，才回退为独立轨道方案。
3. 用 P04 验证三档受伤贴图替换，不动动画。
4. 已用 Z03 的三张书套验证图鉴、头盔损伤与脱落；后续只补跨场景回归。
5. 最后处理共享面最广的 Z01，避免一次改动影响所有基础敌人却没有对照图。

这套顺序把“单张静态替换”“分段耐久”“装备层”和“共享骨架”分开验证，便于出现问题时定位回退点。
